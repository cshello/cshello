#!/usr/bin/env bash

# Use wrapercode
source ../../../../cshello/setup.bash

MD_BASENAME=$(basename "$(pwd)")



cat << EOF > 










cat << EOF > "${MD_BASENAME}.md"

# ${MD_BASENAME}

## 编写一个Protobuf文件
Protobuf文件中我们一般会包含以下内容：
1. 版本。`synax = "proto2" or "proto3"`
2. 包


EOF


